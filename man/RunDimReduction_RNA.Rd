% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rna.R
\name{RunDimReduction_RNA}
\alias{RunDimReduction_RNA}
\title{Run Dimensionality Reduction (PCA, t-SNE, UMAP)}
\usage{
RunDimReduction_RNA(
  obj,
  method = "PCA",
  layer_name = "data",
  n_hvg = 2000,
  pca_rank = 50,
  use_dims = 1:20,
  tsne_perp = 30,
  tsne_iter = 1000,
  umap_neighbors = 30,
  umap_mindist = 0.3
)
}
\arguments{
\item{obj}{A \code{RNA} object.}

\item{method}{Character. Dimensionality reduction method: "PCA", "t-SNE", or "UMAP".}

\item{layer_name}{Character. Data layer to use: "data" (recommended), "scale.data", "counts", or "filter_counts".}

\item{n_hvg}{Integer. Number of highly variable genes to calculate (for PCA only).}

\item{pca_rank}{Integer. Number of principal components to compute.}

\item{use_dims}{Integer vector. Which PCs to use for t-SNE/UMAP downstream (e.g., 1:20).}

\item{tsne_perp}{Numeric. Perplexity parameter for t-SNE.}

\item{tsne_iter}{Integer. Maximum iterations for t-SNE.}

\item{umap_neighbors}{Integer. Number of nearest neighbors for UMAP.}

\item{umap_mindist}{Numeric. Minimum distance parameter for UMAP.}
}
\value{
A \code{RNA} object with embedded coordinates.
}
\description{
Computes Highly Variable Genes (HVG) and performs PCA, t-SNE, or UMAP on the specified data layer.
}
\examples{
# 1. Generate object and process QC
set.seed(42)
mock_counts <- matrix(rnbinom(400, mu = 10, size = 1), nrow = 20, ncol = 20)
rownames(mock_counts) <- paste0("Gene_", 1:20)
colnames(mock_counts) <- paste0("Cell_", 1:20)
rna_obj <- Build_RNAObject(mock_counts, min_cells = 0, min_features = 0)
rna_obj <- ProcessQC_RNA(rna_obj, min_nCount = 0, min_nFeature = 0, do_scale = FALSE)

# 2. Run PCA dimensionality reduction
rna_obj <- RunDimReduction_RNA(
  obj = rna_obj,
  method = "PCA",
  n_hvg = 10,
  pca_rank = 3 # Use small rank for tiny mock data
)
head(rna_obj$reductions$pca)
}
