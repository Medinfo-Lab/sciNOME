% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rna.R
\name{Build_RNAObject}
\alias{Build_RNAObject}
\title{Build a Complete RNA Object}
\usage{
Build_RNAObject(
  expr_mat,
  meta_data = NULL,
  meta_id_col = NULL,
  meta_group_cols = NULL,
  min_cells = 0,
  min_features = 0,
  mt_pattern = "^[Mm][Tt]-",
  project_name = "RNA_Project"
)
}
\arguments{
\item{expr_mat}{Expression matrix (data.frame or matrix), rows are genes, columns are samples.}

\item{meta_data}{Metadata data.frame for sample grouping.}

\item{meta_id_col}{Character. Column in \code{meta_data} matching matrix column names. Defaults to ordered matching if NULL.}

\item{meta_group_cols}{Character vector. Specific metadata columns to add (Defaults to NULL, adding all columns).}

\item{min_cells}{Integer. Pre-filtering: keep genes expressed in at least this many cells.}

\item{min_features}{Integer. Pre-filtering: keep cells expressing at least this many genes.}

\item{mt_pattern}{Character string. Regex pattern for mitochondrial genes. Default is "^[Mm][Tt]-".}

\item{project_name}{Character. Project name.}
}
\value{
A built and pre-filtered \code{RNA} object.
}
\description{
One-step function to generate a RNA object directly from an expression matrix and a metadata data.frame.
Automatically handles data cleaning, NA replacement, pre-filtering, and QC metrics calculation.
}
