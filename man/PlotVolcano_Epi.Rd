% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{PlotVolcano_Epi}
\alias{PlotVolcano_Epi}
\title{Plot Volcano Plot for Differential Epigenetic Analysis}
\usage{
PlotVolcano_Epi(
  df,
  metric_col = "Diff",
  p_col = "P.Value",
  feature_col = "chrdata",
  th_effect = 1,
  th_p = 0.05,
  top_n = 10,
  title = "Volcano Plot",
  custom_xlab = NULL
)
}
\arguments{
\item{df}{A \code{data.frame} containing differential analysis results.}

\item{metric_col}{Character. The column name for the effect size (X-axis). Default is "Diff".}

\item{p_col}{Character. The column name for the P-values (Y-axis). Default is "P.Value".}

\item{feature_col}{Character. The column name for feature names (labels). Default is "chrdata".}

\item{th_effect}{Numeric. The absolute threshold for the effect size (e.g., logFC > 1). Default is 1.0.}

\item{th_p}{Numeric. The threshold for statistical significance (e.g., P-value < 0.05). Default is 0.05.}

\item{top_n}{Integer. Number of top significant features to label. Default is 10.}

\item{title}{Character. The main title of the plot. Default is "Volcano Plot".}

\item{custom_xlab}{Character or Expression. Custom label for the X-axis. If NULL, auto-generated based on \code{metric_col}.}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a publication-quality volcano plot to visualize differential
methylation/epigenetic regions (DMRs). Supports dynamic effect size metrics
(e.g., logFC, Difference, Z-Scores).
}
\examples{
# 1. Create mock Differential Epigenetic Analysis (DEA) results
set.seed(42)
mock_epi_dea <- data.frame(
  chrdata = paste0("chr1:", 1000:(1000+99)),
  Diff = rnorm(100, mean = 0, sd = 0.8),
  P.Value = runif(100, min = 0, max = 0.5)
)
# Force some significant points
mock_epi_dea$Diff[1:3] <- c(1.5, 2.0, -1.8)
mock_epi_dea$P.Value[1:3] <- c(0.001, 0.0005, 0.002)

# 2. Plot Epigenetic Volcano Plot
p_vol <- PlotVolcano_Epi(
  df = mock_epi_dea,
  metric_col = "Diff", p_col = "P.Value", feature_col = "chrdata",
  th_effect = 1.0, th_p = 0.05, top_n = 3
)
p_vol
}
