% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rna.R
\name{RunClustering_RNA}
\alias{RunClustering_RNA}
\title{Run Clustering for RNA Object}
\usage{
RunClustering_RNA(
  obj,
  reduction = "pca",
  method = "graph",
  use_dims = 1:20,
  cluster_res = 0.8,
  k_neighbors = 20,
  cluster_k = 5
)
}
\arguments{
\item{obj}{A \code{RNA} object (Requires prior dimensionality reduction).}

\item{reduction}{Character. Which reduction to use: "pca" (recommended), "umap", or "tsne".}

\item{method}{Character. Clustering algorithm: "graph" (KNN + Leiden) or "hierarchical".}

\item{use_dims}{Integer vector. Dimensions to extract from the reduction (e.g., 1:20).}

\item{cluster_res}{Numeric. Resolution parameter for Leiden algorithm (larger values = more clusters).}

\item{k_neighbors}{Integer. Number of neighbors for K-NN graph construction.}

\item{cluster_k}{Integer. Fixed number of clusters (k) for hierarchical clustering.}
}
\value{
A \code{RNA} object with \code{Auto_Cluster} added to its \code{filter_meta.data}.
}
\description{
Performs cell/sample clustering using either a Graph-based (Leiden) approach or Hierarchical clustering.
}
\examples{
# 1. Prepare data and run dimensionality reduction
set.seed(123)
mock_counts <- matrix(rpois(400, lambda = 10), nrow = 20, ncol = 20)
rownames(mock_counts) <- paste0("Gene_", 1:20)
colnames(mock_counts) <- paste0("Cell_", 1:20)
rna_obj <- Build_RNAObject(mock_counts, min_cells = 0, min_features = 0)
rna_obj <- ProcessQC_RNA(rna_obj, min_nCount = 0, min_nFeature = 0, do_scale = FALSE)
rna_obj <- RunDimReduction_RNA(rna_obj, method = "PCA", n_hvg = 15, pca_rank = 5)

# 2. Run Clustering (Hierarchical is very fast and stable for tiny data)
rna_obj <- RunClustering_RNA(
  obj = rna_obj,
  reduction = "pca",
  method = "hierarchical",
  cluster_k = 2
)
table(rna_obj$filter_meta.data$Auto_Cluster)
}
