% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rna.R
\name{CreateRNAObject}
\alias{CreateRNAObject}
\title{Create a Lightweight RNA Object}
\usage{
CreateRNAObject(
  counts,
  project_name = "RNA_Project",
  mt_pattern = "^[Mm][Tt]-"
)
}
\arguments{
\item{counts}{A sparse matrix or data.frame containing raw expression counts.}

\item{project_name}{Character string specifying the project name. Default is "RNA_Project".}

\item{mt_pattern}{Character string. Regex pattern to identify mitochondrial genes. Default is "^[Mm][Tt]-".}
}
\value{
A \code{RNA} object containing initialized assays and basic QC metrics.
}
\description{
Internal core function to initialize a RNA object from a raw counts matrix.
Automatically calculates basic QC metrics including percent_mt.
}
\examples{
# Create a mock count matrix (10 genes x 10 cells)
set.seed(42)
mock_counts <- matrix(rpois(100, lambda = 5), nrow = 10, ncol = 10)
rownames(mock_counts) <- paste0("Gene_", 1:10)
colnames(mock_counts) <- paste0("Cell_", 1:10)

# Initialize the RNA object
rna_obj <- CreateRNAObject(counts = mock_counts, project_name = "Mock_Project")
print(class(rna_obj))
head(rna_obj$meta.data)
}
