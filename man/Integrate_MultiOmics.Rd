% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_omics.R
\name{Integrate_MultiOmics}
\alias{Integrate_MultiOmics}
\title{Integrate RNA, DNA Methylation (CpG), and Chromatin Accessibility (GpC) Data}
\usage{
Integrate_MultiOmics(
  mode = c("tri", "rna_cpg", "rna_gpc", "cpg_gpc"),
  target_group,
  meta_df,
  group_col = "group1",
  region_df,
  rna_obj = NULL,
  rna_id_col = "sample",
  rna_diff_df = NULL,
  rna_pval_col = "p_val",
  rna_logfc_col = "avg_log2FC",
  rna_pval_th = 0.05,
  rna_logfc_th = 0.5,
  cpg_mat = NULL,
  cpg_id_col = "CpG_level",
  cpg_diff_df = NULL,
  cpg_pval_col = "P.Value",
  cpg_diff_col = "Diff",
  cpg_pval_th = 0.05,
  cpg_diff_th = 0.1,
  gpc_mat = NULL,
  gpc_id_col = "GpC_level",
  gpc_diff_df = NULL,
  gpc_pval_col = "P.Value",
  gpc_diff_col = "Diff",
  gpc_pval_th = 0.05,
  gpc_diff_th = 0.1
)
}
\arguments{
\item{mode}{Character. Integration mode: "tri" (all 3), "rna_cpg", "rna_gpc", or "cpg_gpc".}

\item{target_group}{Character. The biological group/condition to calculate averages for.}

\item{meta_df}{Data.frame. The global metadata linking all omics samples (e.g., "ALL" sheet).}

\item{group_col}{Character. The column name in \code{meta_df} containing group info (e.g., "group1").}

\item{region_df}{Data.frame. Genomic region annotations (must contain chr, start, end, and gene id).}

\item{rna_obj}{Object. The scRNA-seq object.}

\item{rna_id_col}{Character. Column in \code{meta_df} matching RNA cell/sample names.}

\item{rna_diff_df}{Data.frame. Optional. RNA differential analysis results.}

\item{rna_pval_col}{Character. P-value column in \code{rna_diff_df}.}

\item{rna_logfc_col}{Character. logFC column in \code{rna_diff_df}.}

\item{rna_pval_th}{Numeric. P-value threshold for RNA.}

\item{rna_logfc_th}{Numeric. Absolute logFC threshold for RNA.}

\item{cpg_mat}{Matrix/Data.frame. CpG level matrix (Rows = Regions, Cols = Samples).}

\item{cpg_id_col}{Character. Column in \code{meta_df} matching CpG matrix columns.}

\item{cpg_diff_df}{Data.frame. Optional. CpG DMR analysis results.}

\item{cpg_pval_col}{Character. P-value column in \code{cpg_diff_df}.}

\item{cpg_diff_col}{Character. Difference column in \code{cpg_diff_df}.}

\item{cpg_pval_th}{Numeric. P-value threshold for CpG.}

\item{cpg_diff_th}{Numeric. Absolute difference threshold for CpG.}

\item{gpc_mat}{Matrix/Data.frame. GpC level matrix.}

\item{gpc_id_col}{Character. Column in \code{meta_df} matching GpC matrix columns.}

\item{gpc_diff_df}{Data.frame. Optional. GpC DMR analysis results.}

\item{gpc_pval_col}{Character. P-value column in \code{gpc_diff_df}.}

\item{gpc_diff_col}{Character. Difference column in \code{gpc_diff_df}.}

\item{gpc_pval_th}{Numeric. P-value threshold for GpC.}

\item{gpc_diff_th}{Numeric. Absolute difference threshold for GpC.}
}
\value{
A merged \code{data.frame} containing integrated omics levels.
}
\description{
Merges multiple omics layers based on genomic region annotations and Gene IDs.
Uses a single global metadata table to safely map samples across different omics matrices.
Optionally filters features based on differential analysis results.
}
\examples{
# 1. Create mock metadata
meta_df <- data.frame(
  group1 = c("Tumor", "Tumor", "Normal"),
  sample = c("cell1", "cell2", "cell3"),
  CpG_level = c("samp1", "samp2", "samp3")
)

# 2. Create mock region annotation
region_df <- data.frame(
  chr = c("chr1", "chr2"),
  start = c(100, 200),
  end = c(150, 250),
  gene_id = c("GeneA", "GeneB")
)

# 3. Create mock RNA object
rna_mat <- matrix(
  c(10, 12, 2, 5, 6, 1), nrow = 2, byrow = TRUE,
  dimnames = list(c("GeneA", "GeneB"), c("cell1", "cell2", "cell3"))
)
rna_obj <- list(assays = list(RNA = list(data = rna_mat)))

# 4. Create mock CpG matrix (rownames match chr:start-end from region_df)
cpg_mat <- matrix(
  c(0.8, 0.9, 0.1, 0.7, 0.8, 0.2), nrow = 2, byrow = TRUE,
  dimnames = list(c("chr1:100-150", "chr2:200-250"), c("samp1", "samp2", "samp3"))
)

# 5. Run Integration
merged_res <- Integrate_MultiOmics(
  mode = "rna_cpg",
  target_group = "Tumor",
  meta_df = meta_df,
  group_col = "group1",
  region_df = region_df,
  rna_obj = rna_obj, rna_id_col = "sample",
  cpg_mat = cpg_mat, cpg_id_col = "CpG_level"
)

head(merged_res)

}
