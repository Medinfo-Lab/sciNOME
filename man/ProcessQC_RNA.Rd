% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rna.R
\name{ProcessQC_RNA}
\alias{ProcessQC_RNA}
\title{Process QC, Filtering, and Normalization for RNA Object}
\usage{
ProcessQC_RNA(
  obj,
  mt_pattern = "^MT-",
  min_nCount = 500,
  max_nCount = 50000,
  min_nFeature = 200,
  max_nFeature = 8000,
  max_mt = 15,
  norm_method = "LogNormalize",
  do_scale = TRUE
)
}
\arguments{
\item{obj}{A \code{RNA} object.}

\item{mt_pattern}{Character. Regex pattern to identify mitochondrial genes (e.g., "^MT-" for human, "^mt-" for mouse).}

\item{min_nCount}{Numeric. Minimum UMI count threshold.}

\item{max_nCount}{Numeric. Maximum UMI count threshold.}

\item{min_nFeature}{Numeric. Minimum number of detected genes.}

\item{max_nFeature}{Numeric. Maximum number of detected genes.}

\item{max_mt}{Numeric. Maximum allowed mitochondrial percentage (e.g., 15 means 15\%).}

\item{norm_method}{Character. Normalization method: "LogNormalize", "LogCPM", or "TPM".}

\item{do_scale}{Logical. Whether to center and scale the data (highly memory-intensive).}
}
\value{
A \code{RNA} object after QC, filtering, and normalization.
}
\description{
Performs mitochondrial proportion calculation, rigorous cell filtering,
sparse matrix normalization (LogNormalize, LogCPM, or TPM), and optional data scaling.
}
\examples{
# 1. Create mock data (include mock mitochondrial genes)
set.seed(123)
mock_counts <- matrix(rpois(100, lambda = 10), nrow = 10, ncol = 10)
rownames(mock_counts) <- c("MT-ND1", "MT-ND2", paste0("Gene_", 3:10))
colnames(mock_counts) <- paste0("Cell_", 1:10)

# 2. Build basic object
rna_obj <- Build_RNAObject(mock_counts, min_cells = 0, min_features = 0)

# 3. Run QC and Normalization (use relaxed thresholds for mock data)
rna_obj <- ProcessQC_RNA(
  obj = rna_obj,
  mt_pattern = "^MT-",
  min_nCount = 0, min_nFeature = 0, max_mt = 100,
  norm_method = "LogNormalize",
  do_scale = FALSE # Keep false to save time in example
)
dim(rna_obj$assays$RNA$data)
}
